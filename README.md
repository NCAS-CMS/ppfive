# ppfive

A pp and fields file reader which mimics relevant elements of hte `pyfive` high-level API
with support for lazy metadata loading and parallel data loading when necessary.

Under Construction!!
